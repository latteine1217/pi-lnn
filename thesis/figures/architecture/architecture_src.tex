% PI-CON architecture diagram — TikZ vector source.
% Compile:  pdflatex architecture_src.tex  ->  architecture_src.pdf（已緊裁，直接 include）
\documentclass[tikz, border=2pt]{standalone}
\usepackage{amsmath}
\usetikzlibrary{arrows.meta,positioning,fit,backgrounds,calc}

\definecolor{picon}{HTML}{0072B2}
\definecolor{piconfill}{HTML}{E9F2FB}
\definecolor{inkline}{HTML}{1A1A1A}
\definecolor{lanefill}{HTML}{F5F7F9}
\definecolor{laneedge}{HTML}{DDE4EA}
\definecolor{incol}{HTML}{009E73}    % Okabe-Ito bluish-green:     輸入
\definecolor{outcol}{HTML}{D55E00}   % Okabe-Ito vermillion:       輸出
\definecolor{traincol}{HTML}{CC79A7} % Okabe-Ito reddish-purple:   訓練迴路
\definecolor{zonecol}{HTML}{6E7B87}  % 分區標題：組織用，需退到內容之後

\newcommand{\ttl}[1]{{\large\bfseries #1}}
\newcommand{\btl}[1]{\textcolor{picon}{\large\bfseries #1}}
\newcommand{\sub}[1]{{\normalsize #1}}

\begin{document}%
\hyphenpenalty=10000\exhyphenpenalty=10000\relax
\begin{tikzpicture}[
  font=\sffamily,
  >={Latex[length=1.7mm,width=1.5mm]},
  box/.style={rounded corners=3pt, draw=inkline, line width=0.5pt, fill=white,
              align=center, inner sep=5pt, minimum height=1.2cm, text=inkline},
  picon/.style={rounded corners=3pt, draw=picon, line width=1.1pt, fill=piconfill,
                align=center, inner sep=5pt, minimum height=1.2cm, text=inkline},
  badge/.style={circle, fill=picon, text=white, inner sep=0pt, minimum size=5.4mm,
                font=\sffamily\bfseries\small, draw=white, line width=0.5pt},
  zone/.style={font=\sffamily\bfseries\normalsize, text=zonecol, anchor=west},
  arr/.style={->, draw=inkline, line width=0.55pt},
  % 訓練迴路：整條以顏色與前向 signal flow 分流（虛線僅為次要區隔）
  tarr/.style={->, draw=traincol, line width=0.7pt, dashed},
  tline/.style={draw=traincol, line width=0.7pt, dashed},
  elab/.style={font=\sffamily\small, text=inkline, inner sep=1.5pt, fill=white},
  tlab/.style={font=\sffamily\small, text=traincol, inner sep=1.5pt, fill=white},
  io/.style={align=center, inner sep=3pt},   % 輸入/輸出：不加框，僅以顏色標示
]

% ---- branch row ----
\node[io, text width=2.4cm]   (obs)  at (0,0)      {\textcolor{incol}{\ttl{Observations}}\\[1pt]\textcolor{incol}{\sub{sensor values,\\ positions + time}}};
% CfC 置於 Trunk Feature 右側：錯開 x 才有淨空的垂直通道讓 update θ 接回。
\node[picon, text width=2.6cm](cfc)  at (7.4,0)    {\btl{CfC Memory}\\[1pt]\sub{token attention,\\ causal scan dt}};

% ---- trunk row ----
\node[io, text width=2.4cm]   (qry)  at (0,-3.2)   {\textcolor{incol}{\ttl{Query}}\\[1pt]\textcolor{incol}{\sub{$(x,y,t_q,c)$}}};
\node[box, text width=2.7cm]  (tf)   at (5.6,-3.2) {\ttl{Trunk Feature}\\[1pt]\sub{Fourier + temporal anchor,\\ dt\_to\_query}};

% ---- interaction ----
\node[picon, text width=2.9cm](ca)   at (11.0,-1.6){\btl{Cross-Attention Readout}\\[1pt]\sub{isotropic distance bias}};

% ---- output ----
\node[box, text width=2.2cm]  (bb)   at (14.0,0.0) {\ttl{Branch Basis}};
\node[box, text width=2.2cm]  (tb)   at (14.0,-3.2){\ttl{Trunk Basis}};
% DeepONet fusion 是沿 rank 的 inner product（外加 7 個純量：temperature +
% per-component affine），不值得一個方塊 → 降為兩條 basis 匯流處的箭頭標籤。
\coordinate (mrg) at (16.0,-1.6);
\node[io, text width=1.7cm]   (fld)  at (18.6,-1.6){\textcolor{outcol}{\ttl{Field}}\\[1pt]\textcolor{outcol}{\sub{$u,v,p$}}};
\node[picon, text width=3.0cm](loss) at (18.6,-6.2){\btl{Loss}\\[1pt]\sub{AL continuity constraint,\\ sensor MSE + NS residual, GradNorm}};

% ---- optimizer feedback ----
% 置於左下：訓練列左右各一箱，中間即回饋線；避免右半壅塞、左下留白。
\node[box, text width=3.2cm]  (opt)  at (2.2,-6.2){\ttl{SOAP + Schedule-Free}\\[1pt]\sub{preconditioned updates}};

% ---- badges (straddle top-left corner) ----
\node[badge] at (cfc.north west)  {1};
\node[badge] at (ca.north west)   {2};
\node[badge] at (loss.north west) {3};

% ---- zone headers（灰底泳道已移除：僅以標題分區，減輕視覺重量）----
\node[zone] at (-1.2,1.3)  {BRANCH PATH: SPARSE SENSOR MEMORY};
\node[zone] at (-1.2,-1.9) {TRUNK PATH: QUERY FEATURE};
\node[zone] at (9.55,1.3)  {INTERACTION};
\node[zone] at (12.9,1.3)  {OUTPUT AND LOSS};

% ---- arrows ----
\draw[arr] (obs) -- (cfc) node[elab, midway, above] {Fourier embedding};
\draw[arr] (cfc.east) -| (ca.north) node[elab, pos=0.28, above] {K,V};
\draw[arr] (qry) -- (tf);
\draw[arr] (tf.east) -| (ca.south) node[elab, pos=0.28, below] {Q};
\draw[arr] (tf.east) -- (tb.west);
\draw[arr] (ca.east) -| (bb.south);
\draw[draw=inkline, line width=0.55pt] (bb.east) -| (mrg);
\draw[draw=inkline, line width=0.55pt] (tb.east) -| (mrg);
\fill[inkline] (mrg) circle (1.5pt);
\draw[arr] (mrg) -- (fld) node[elab, midway, above] {inner product};
% ---- 訓練迴路（紫）：梯度下行、參數更新回饋到兩條 path 上的可訓練模組 ----
\draw[tarr] (fld.south) -- (loss.north) node[tlab, pos=0.55] {autograd};
\draw[tarr] (loss.west) -- (opt.east) node[tlab, pos=0.12, above] {optimize};
\draw[tline] (opt.north) -- (2.2,-4.8) -- (7.4,-4.8);
\node[tlab] at (3.3,-4.62) {update $\theta$};   % 置於 Trunk Feature 左緣之外，避開箱底
\draw[tarr] (5.6,-4.8) -- (tf.south);
\draw[tarr] (7.4,-4.8) -- (cfc.south);

\end{tikzpicture}%
\end{document}

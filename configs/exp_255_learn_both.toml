# configs/exp_255_learn_both.toml
# What: EXP-255 (task 3) — 同時學 forcing 振幅 A 與波數 k_f（兩者都不知）
#
# Why: 工程場景：force 完全未知（只知道是某種週期性外力）。
#      最 ill-conditioned 配置，測試 PI-CON identifiability 上限。
#
# Hypothesis: 雙學 = 1 線性 unknown + 1 非線性 unknown，PDE residual 仍可 identify
#             但收斂較慢。預期 A∈[0.09, 0.11] 且 k_f∈[1.9, 2.1]，KE rel-err 8-15%。
#
# Falsifiability:
#   (a) A, k_f 雙雙收斂真值 ±10% + KE < 15% → 雙學 identifiability 成立
#   (b) 一個收斂一個崩 → coupling 不對稱（gradient 互打）
#   (c) 收斂到 scaled 局部解（A→0.05, k_f→4 但 u 場合理）→ 多解問題
#
# 設定對齊 EXP-252（option C）唯一改動 forcing flag + init.

[train]
sensor_jsons = [
  "data/kolmogorov_sensors/re10000/sensors_qrpivot_K100_N256_t0-5_si100_les_n256_T50standalone.json",
]
sensor_npzs = [
  "data/kolmogorov_sensors/re10000/sensors_qrpivot_K100_N256_t0-5_si100_les_n256_T50standalone_dns_values.npz",
]
dns_paths = ["data/dns/kolmogorov_dns_fp64_etdrk4_Re10000_N256_T5_dt2p5e4_si100_ds4.npy"]
re_values = [10000.0]
observed_sensor_channels = ["u", "v"]

fourier_harmonics = 16
fourier_embed_dim = 128
use_temporal_anchor = true
T_total = 5.0
temporal_anchor_harmonics = 2
d_model = 256
d_time = 16
num_spatial_cfc_layers = 1
num_temporal_cfc_layers = 1
num_token_attention_layers = 2
token_attention_heads = 4
num_query_mlp_layers = 1
query_mlp_hidden_dim = 256
output_head_gain = 1.0
operator_rank = 256

data_loss_weight = 1.0
physics_loss_weight = 0.057
continuity_weight = 0.0
poisson_loss_weight = 0.0
t_early_weight = 10.0
t_early_threshold = 0.05
physics_collocation_strategy = "random"
time_marching = true
time_marching_start = 0.5
time_marching_warmup = 0.3
domain_length = 1.0
kolmogorov_k_f = 2.0          # 真值（僅 metric 對照用）
kolmogorov_A = 0.1            # 真值（僅 metric 對照用）

# Learn both A and k_f
learn_forcing_A = true
learn_forcing_k_f = true
forcing_A_init = 0.05         # 50% of truth
forcing_k_f_init = 4.0        # 2× truth
forcing_k_f_min = 1.0
forcing_k_f_max = 8.0

iterations = 10000
num_physics_points = 1024
learning_rate = 1e-3
lr_schedule = "soap"
use_schedule_free = true
soap_betas = [0.9, 0.999]
soap_precondition_frequency = 2
lr_warmup_steps = 2000
soap_use_step_decay = true
lr_decay_steps = 2000
lr_decay_gamma = 0.9
min_learning_rate = 1e-6
max_grad_norm = 1.0

use_gradnorm = true
gradnorm_update_freq = 1000
gradnorm_ema_momentum = 0.9
gradnorm_tasks = ["data", "ns_u", "ns_v", "cont"]
gradnorm_init_weights = [1.0, 0.057, 0.057, 0.01]
gradnorm_min_weight = 0.0
gradnorm_max_weight = 0.0

use_augmented_lagrangian = true
al_constraints = ["cont"]
al_init_lambda = 0.0
al_rho = 0.1
al_update_freq = 100
al_lambda_clip = 10.0
al_ema_momentum = 0.5
al_allow_cont_in_gradnorm = true

checkpoint_period = 500
seed = 42
device = "mps"
use_sensor_physics = false

artifacts_dir = "artifacts/kolmogorov/deeponet-cfc-re10000-exp255-learn-both"

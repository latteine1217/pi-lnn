# configs/exp_038_re10000_poisson.toml
# What: EXP-038 — 與 EXP-031 相同設定（d=128, random-32, SOAP+SF 3k），
#       新增壓力 Poisson 方程作為額外 physics 約束。
#
# Why: EXP-031 的 primitive-variable NS loss 中，p 沒有資料監督，
#      模型可藉由任意調整 p 讓 momentum residual 歸零（壓力自由度問題）。
#      壓力 Poisson 方程 ∇²p = -(∂u/∂x)² - (∂v/∂y)² - 2(∂u/∂y)(∂v/∂x)
#      直接從 NS + ∇·u=0 推導，要求 p 必須與 u, v 的速度梯度結構一致。
#      加入此約束後，momentum loss 中 p 不能再自由漂移，
#      使 momentum 殘差真正約束 u, v 的動力學。
#
# Hypothesis: 壓力 Poisson 去除 p 的自由度後，momentum loss 能有效指導
#             Re=10000 的動量方程，KE rel-err < 36%（優於 EXP-031 的 39.4%）。
# Falsifiability: 若 KE > 0.39，表示壓力自由度不是主要問題，
#                 Re=10000 的瓶頸在模型表達力或資訊量本身。

[train]
sensor_jsons = [
  "data/kolmogorov_sensors/re10000/sensors_qrpivot_K100_N256_t0-5_si100.json",
]
sensor_npzs = [
  "data/kolmogorov_sensors/re10000/sensors_qrpivot_K100_N256_t0-5_si100_dns_values.npz",
]
dns_paths = [
  "data/dns/kolmogorov_dns_fp64_etdrk4_Re10000_N256_T5_dt2p5e4_si100_ds4.npy",
]
re_values = [10000.0]
observed_sensor_channels = ["u", "v"]

fourier_harmonics = 16
use_temporal_anchor = true
T_total = 5.0
temporal_anchor_harmonics = 2
d_model = 128
d_time = 16
num_spatial_cfc_layers = 1
num_temporal_cfc_layers = 1
num_token_attention_layers = 2
token_attention_heads = 4
num_query_mlp_layers = 1
query_mlp_hidden_dim = 128
output_head_gain = 1.0
operator_rank = 128

data_loss_weight = 1.0
physics_loss_weight = 0.01
physics_loss_warmup_steps = 0
physics_loss_ramp_steps = 0
continuity_weight = 1.0
poisson_loss_weight = 1.0

physics_collocation_strategy = "random"
physics_residual_normalize = false
num_physics_points = 32

time_marching = true
time_marching_start = 0.5
time_marching_warmup = 0.5
domain_length = 1.0
kolmogorov_k_f = 2.0
kolmogorov_A = 0.1

iterations = 3000
num_query_points = 128
learning_rate = 3e-3
weight_decay = 1e-4
lr_schedule = "soap"
use_schedule_free = true
soap_precondition_frequency = 10
min_learning_rate = 1e-6
max_grad_norm = 1.0
checkpoint_period = 100
seed = 42
device = "mps"
artifacts_dir = "artifacts/deeponet-cfc-re10000-poisson-3000"

# configs/exp_065_re10000_xlarge_trunk2.toml
# What: EXP-065 — EXP-064 + trunk query MLP 深度 1→2 層。
#
# Why: EXP-064 falsified sensor physics continuity 假設，確認 band_mid/high ≈100%
#      的根源是模型表達力（trunk 深度）或感測器策略，而非 physics 約束密度。
#      Trunk MLP 是 spatial query embedding 進入 cross-attention 前的唯一非線性層；
#      1 層 ResidualMLPBlock 可能不足以將 LearnableFourierEmb 的高頻基函數
#      組合成有意義的中高頻空間表示。加深至 2 層使非線性容量加倍。
#
# 唯一改動（vs EXP-064）：
#   num_query_mlp_layers = 1 → 2
#   （每加一層 ResidualMLPBlock(256,256) 約增加 132k params）
#
# Hypothesis: 更深 trunk query MLP 使模型具備表達 k>5 空間模態的能力；
#             band_mid/high@t=5 < 100%；KE ≤ 7.80%（EXP-064）
# Falsifiability: 若 band_mid/high@t=5 仍 ≈100% →
#                 確認根本瓶頸為 K=100 sensor 資訊論硬上限，
#                 架構表達力非解法；唯一出路是增加 sensor 覆蓋或引入高頻先驗。

[train]
sensor_jsons = [
  "data/kolmogorov_sensors/re10000/sensors_qrpivot_K100_N256_t0-5_si100.json",
]
sensor_npzs = [
  "data/kolmogorov_sensors/re10000/sensors_qrpivot_K100_N256_t0-5_si100_dns_values.npz",
]
dns_paths = [
  "data/dns/kolmogorov_dns_fp64_etdrk4_Re10000_N256_T5_dt2p5e4_si100_ds4.npy",
]
re_values = [10000.0]
observed_sensor_channels = ["u", "v"]

fourier_harmonics = 16
fourier_embed_dim = 128
use_temporal_anchor = true
T_total = 5.0
temporal_anchor_harmonics = 2
d_model = 256
d_time = 16
num_spatial_cfc_layers = 1
num_temporal_cfc_layers = 1
num_token_attention_layers = 2
token_attention_heads = 4
num_query_mlp_layers = 2
query_mlp_hidden_dim = 256
output_head_gain = 1.0
operator_rank = 256

data_loss_weight = 1.0
physics_loss_weight = 0.01
physics_loss_warmup_steps = 0
physics_loss_ramp_steps = 0
continuity_weight = 1.0
poisson_loss_weight = 0.0

t_early_weight = 10.0
t_early_threshold = 0.05

physics_collocation_strategy = "random"

time_marching = true
time_marching_start = 0.5
time_marching_warmup = 0.3
domain_length = 1.0
kolmogorov_k_f = 2.0
kolmogorov_A = 0.1

iterations = 10000
num_physics_points = 64
learning_rate = 1e-3
weight_decay = 0.0
lr_schedule = "soap"
use_schedule_free = true
soap_betas = [0.9, 0.999]
soap_precondition_frequency = 2
lr_warmup_steps = 2000
soap_use_step_decay = true
lr_decay_steps = 2000
lr_decay_gamma = 0.9
min_learning_rate = 1e-6
max_grad_norm = 1.0

use_gradnorm = true
gradnorm_update_freq = 1000
gradnorm_ema_momentum = 0.9
gradnorm_init_weights = [1.0, 0.01, 0.01, 0.01]

checkpoint_period = 500
seed = 42
device = "mps"
use_sensor_physics = true
num_sensor_physics_time_samples = 1
sensor_physics_start_step = 1000

artifacts_dir = "artifacts/deeponet-cfc-re10000-exp065-trunk2"

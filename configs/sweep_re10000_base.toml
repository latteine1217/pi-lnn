# configs/sweep_re10000_base.toml
# What: Optuna sweep 的 Re=10000 基準設定（基於 EXP-043 架構）。
# Why:  EXP-043（d=256, SOAP+SF, 5000 steps）目前 KE=27.2%，是 Re=10000 主線。
#       sweep 繼承此架構，不 resume checkpoint（從頭訓練以公平比較），
#       迭代數縮短至 1500 步用於快速排名（top-3 再以完整 3000 步重跑）。

[train]
sensor_jsons = [
  "data/kolmogorov_sensors/re10000/sensors_qrpivot_K100_N256_t0-5_si100.json",
]
sensor_npzs = [
  "data/kolmogorov_sensors/re10000/sensors_qrpivot_K100_N256_t0-5_si100_dns_values.npz",
]
dns_paths = [
  "data/dns/kolmogorov_dns_fp64_etdrk4_Re10000_N256_T5_dt2p5e4_si100_ds4.npy",
]
re_values = [10000.0]
observed_sensor_channels = ["u", "v"]

# 架構（固定，與 EXP-043 相同）
fourier_harmonics = 16
use_temporal_anchor = true
T_total = 5.0
temporal_anchor_harmonics = 2
d_model = 256
d_time = 16
num_spatial_cfc_layers = 1
num_temporal_cfc_layers = 1
num_token_attention_layers = 2
token_attention_heads = 4
num_query_mlp_layers = 1
query_mlp_hidden_dim = 256
output_head_gain = 1.0
operator_rank = 256

# 物理損失（sweep 可覆蓋 physics_loss_weight / continuity_weight）
data_loss_weight = 1.0
physics_loss_weight = 0.01
physics_loss_warmup_steps = 0
physics_loss_ramp_steps = 0
continuity_weight = 1.0
poisson_loss_weight = 0.0
physics_collocation_strategy = "random"
physics_residual_normalize = false
num_physics_points = 32

# 時間推進
time_marching = true
time_marching_start = 0.5
time_marching_warmup = 0.5
domain_length = 1.0
kolmogorov_k_f = 2.0
kolmogorov_A = 0.1

# 訓練（sweep 會覆蓋 learning_rate / weight_decay / soap_precondition_frequency）
iterations = 1500
num_query_points = 128
learning_rate = 3e-3
weight_decay = 1e-4
lr_schedule = "soap"
use_schedule_free = true
soap_precondition_frequency = 10
min_learning_rate = 1e-6
max_grad_norm = 1.0
checkpoint_period = 0      # sweep 中不儲存 checkpoint，節省 I/O
seed = 42
device = "mps"
artifacts_dir = "artifacts/sweep/base"
